"""
📉 ГИСТОГРАММА (Histogram)

Описание:
    Показывает распределение непрерывных данных.
    Данные группируются в интервалы (bins), высота столбца = частота.

Когда использовать:
    ✓ Показать распределение данных
    ✓ Найти выбросы и аномалии
    ✓ Понять форму распределения (нормальное, скошенное)
    ✓ Анализ частоты появления значений

Не использовать:
    ✗ Для категориальных данных (используйте bar plot)
    ✗ Для временных рядов
    ✗ Для показа точных значений
"""

import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import numpy as np


def create_histogram(df, output_path='output/histogram.png'):
    """
    Создание гистограммы

    Args:
        df: DataFrame с данными
        output_path: путь для сохранения графика
    """
    print("\n" + "="*80)
    print("📉 ГИСТОГРАММА (Histogram)")
    print("="*80)

    print("\n💡 Гистограмма показывает, как распределены ваши данные.")
    print("   Помогает найти среднее значение, выбросы и понять форму распределения.\n")

    fig, axes = plt.subplots(2, 2, figsize=(15, 10))
    fig.suptitle('Гистограммы: примеры использования', fontsize=16, fontweight='bold')

    # 1. Простая гистограмма
    axes[0, 0].hist(df['Средняя_цена'], bins=30, color='#3498DB',
                   edgecolor='white', linewidth=1.5, alpha=0.7)
    axes[0, 0].set_title('1️⃣ Простая гистограмма')
    axes[0, 0].set_xlabel('Средняя цена (руб.)')
    axes[0, 0].set_ylabel('Частота')
    axes[0, 0].grid(axis='y', alpha=0.3)

    # Добавляем среднее и медиану
    mean_price = df['Средняя_цена'].mean()
    median_price = df['Средняя_цена'].median()
    axes[0, 0].axvline(mean_price, color='red', linestyle='--',
                      linewidth=2, label=f'Среднее: {mean_price:.0f}')
    axes[0, 0].axvline(median_price, color='green', linestyle='--',
                      linewidth=2, label=f'Медиана: {median_price:.0f}')
    axes[0, 0].legend()

    # 2. Сравнение распределений
    for category in df['Категория'].unique()[:3]:  # Берем 3 категории
        cat_data = df[df['Категория'] == category]['Средняя_цена']
        axes[0, 1].hist(cat_data, bins=20, alpha=0.5, label=category, edgecolor='white')
    axes[0, 1].set_title('2️⃣ Сравнение распределений')
    axes[0, 1].set_xlabel('Средняя цена (руб.)')
    axes[0, 1].set_ylabel('Частота')
    axes[0, 1].legend()
    axes[0, 1].grid(axis='y', alpha=0.3)

    # 3. Гистограмма с кривой плотности (KDE)
    n, bins, patches = axes[1, 0].hist(df['Продажи'], bins=30,
                                       color='#E74C3C', alpha=0.6,
                                       edgecolor='white', linewidth=1.5,
                                       density=True)

    # Добавляем кривую плотности
    from scipy import stats
    mu = df['Продажи'].mean()
    sigma = df['Продажи'].std()
    x = np.linspace(df['Продажи'].min(), df['Продажи'].max(), 100)
    axes[1, 0].plot(x, stats.norm.pdf(x, mu, sigma),
                   'k--', linewidth=2, label='Нормальное распределение')

    axes[1, 0].set_title('3️⃣ С кривой плотности')
    axes[1, 0].set_xlabel('Продажи (руб.)')
    axes[1, 0].set_ylabel('Плотность вероятности')
    axes[1, 0].legend()
    axes[1, 0].grid(axis='y', alpha=0.3)

    # 4. Кумулятивная гистограмма
    axes[1, 1].hist(df['Количество'], bins=20, color='#1ABC9C',
                   edgecolor='white', linewidth=1.5, alpha=0.7,
                   cumulative=True)
    axes[1, 1].set_title('4️⃣ Кумулятивная гистограмма')
    axes[1, 1].set_xlabel('Количество товаров')
    axes[1, 1].set_ylabel('Накопленная частота')
    axes[1, 1].grid(axis='y', alpha=0.3)

    plt.tight_layout()
    plt.savefig(output_path, dpi=300, bbox_inches='tight')
    print(f"✅ График сохранен: {output_path}")

    # Статистика
    print("\n📊 Статистика распределения цен:")
    print(f"   • Среднее: {df['Средняя_цена'].mean():.2f} руб.")
    print(f"   • Медиана: {df['Средняя_цена'].median():.2f} руб.")
    print(f"   • Стд. отклонение: {df['Средняя_цена'].std():.2f} руб.")
    print(f"   • Минимум: {df['Средняя_цена'].min():.2f} руб.")
    print(f"   • Максимум: {df['Средняя_цена'].max():.2f} руб.")

    print("\n💡 Совет: Если гистограмма симметрична - распределение близко к нормальному!")

    plt.close()

    return output_path


def get_code_example():
    """Возвращает примеры кода для обучения"""
    return {
        'title': 'Гистограмма (Histogram)',
        'description': 'Показывает распределение значений',
        'when_use': 'Распределение данных, частота значений',
        'examples': [
            {
                'name': '1️⃣ Простая гистограмма',
                'code': '''# Подготовка данных
prices = df['Средняя_цена']

# Создание графика
fig, ax = plt.subplots(figsize=(10, 6))
ax.hist(prices,
        bins=30,              # Количество столбцов
        color='coral',        # Цвет
        edgecolor='black',    # Цвет границ
        alpha=0.7)            # Прозрачность

# Настройка
ax.set_title('Распределение средней цены')
ax.set_xlabel('Средняя цена (руб.)')
ax.set_ylabel('Частота')
ax.grid(True, alpha=0.3, axis='y')

plt.tight_layout()
plt.savefig('histogram_simple.png', dpi=300)'''
            },
            {
                'name': '2️⃣ С кривой плотности',
                'code': '''# Подготовка данных
prices = df['Средняя_цена']

# Создание графика
fig, ax = plt.subplots(figsize=(10, 6))

# Гистограмма
n, bins, patches = ax.hist(prices,
                            bins=30,
                            density=True,      # Нормализация
                            color='lightblue',
                            edgecolor='black',
                            alpha=0.6,
                            label='Гистограмма')

# Кривая плотности (KDE)
from scipy import stats
density = stats.gaussian_kde(prices)
xs = np.linspace(prices.min(), prices.max(), 200)
ax.plot(xs, density(xs),
        'r-', linewidth=2, label='Плотность')

# Настройка
ax.set_title('Распределение с кривой плотности')
ax.set_xlabel('Средняя цена (руб.)')
ax.set_ylabel('Плотность')
ax.legend()
ax.grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('histogram_density.png', dpi=300)'''
            },
            {
                'name': '3️⃣ Сравнение распределений',
                'code': '''# Создание графика
fig, ax = plt.subplots(figsize=(10, 6))

# Гистограммы для каждой категории
categories = df['Категория'].unique()
colors = ['#FF6B6B', '#4ECDC4', '#45B7D1', '#FFA07A']

for i, category in enumerate(categories):
    cat_prices = df[df['Категория'] == category]['Средняя_цена']
    ax.hist(cat_prices,
            bins=20,
            alpha=0.5,              # Полупрозрачно
            label=category,
            color=colors[i],
            edgecolor='white')

# Настройка
ax.set_title('Сравнение распределений цен')
ax.set_xlabel('Средняя цена (руб.)')
ax.set_ylabel('Частота')
ax.legend()
ax.grid(True, alpha=0.3, axis='y')

plt.tight_layout()
plt.savefig('histogram_comparison.png', dpi=300)'''
            },
            {
                'name': '4️⃣ Со статистикой',
                'code': '''# Подготовка данных
prices = df['Средняя_цена']
mean_price = prices.mean()
median_price = prices.median()

# Создание графика
fig, ax = plt.subplots(figsize=(10, 6))

# Гистограмма
ax.hist(prices,
        bins=30,
        color='skyblue',
        edgecolor='black',
        alpha=0.7)

# Линии среднего и медианы
ax.axvline(mean_price, color='red',
           linestyle='--', linewidth=2,
           label=f'Среднее: {mean_price:.0f}')
ax.axvline(median_price, color='green',
           linestyle='--', linewidth=2,
           label=f'Медиана: {median_price:.0f}')

# Настройка
ax.set_title('Распределение с показателями')
ax.set_xlabel('Средняя цена (руб.)')
ax.set_ylabel('Частота')
ax.legend()
ax.grid(True, alpha=0.3, axis='y')

plt.tight_layout()
plt.savefig('histogram_stats.png', dpi=300)'''
            }
        ],
        'tips': [
            '💡 bins определяет количество столбцов',
            '💡 density=True нормализует гистограмму',
            '💡 alpha<1 помогает при наложении',
            '💡 axvline() рисует вертикальные линии',
            '💡 Используйте 20-50 bins для большинства случаев'
        ]
    }
